'use client'

export default function Page() {
  return (
    <main style={{ padding: 24, maxWidth: 720, margin: '0 auto' }}>
      <h1>CTRL — Create Time to Reflect &amp; Listen</h1>
      <p>Build sanity check: page compiles.</p>
    </main>
  )
}
